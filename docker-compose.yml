version: '3.8'

services:
  prediction_api:
    build: ./services/prediction
    container_name: prediction_service
    ports:
      - "8000:8000"
    volumes:
      - ./services/prediction:/app

  explanation_api:
    build: ./services/explanation
    container_name: explanation_service
    ports:
      - "8001:8000"
    volumes:
      - ./services/explanation:/app
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}